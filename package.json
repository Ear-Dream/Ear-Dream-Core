{
  "name": "ear-dream-core",
  "private": true,
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:app": "pnpm --filter @ear-dream/app dev",
    "dev:web": "pnpm setup:mediapipe && pnpm --filter @ear-dream/app web",
    "setup:mediapipe": "node scripts/setup-mediapipe-assets.mjs",
    "setup:model-bundle": "node scripts/setup-model-bundle.mjs",
    "setup:sign-sequences": "cd packages/ear-dream-api && uv run python scripts/build_sign_sequences.py",
    "dev:api": "cd packages/ear-dream-api && uv run fastapi dev app/main.py",
    "setup:https-cert": "node scripts/setup-https-cert.mjs",
    "build:web-mobile": "pnpm setup:mediapipe && cd packages/ear-dream-app && EXPO_PUBLIC_API_URL= npx expo export --platform web",
    "serve:mobile": "node scripts/serve-mobile.mjs --https",
    "typecheck": "pnpm -r --parallel typecheck",
    "lint:api": "cd packages/ear-dream-api && uv run ruff check .",
    "test:api": "cd packages/ear-dream-api && uv run pytest",
    "generate:api-types": "node scripts/generate-api-types.mjs"
  }
}
