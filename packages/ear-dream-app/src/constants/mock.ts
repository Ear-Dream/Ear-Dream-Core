/**
 * 흐름 시뮬레이션용 임시값 · 목업 데이터.
 *
 * 여기 있는 값은 전부 미확정이다. 인식 파이프라인(T-08 이후)이 붙으면
 * 이 파일의 값은 그 값을 쓰는 코드와 함께 제거하거나 실제 값으로 대체한다.
 *
 * 농인 트랙의 수어 인식·문장 생성은 실제 서버 호출로 대체되어 이 파일의 mock 을 더
 * 쓰지 않는다 — 남은 항목은 청인 트랙(STT 미구현) 것과 UI 타이밍 임시값뿐이다.
 */

/**
 * 청인 트랙 "음성 인식 중" 화면 체류 시간(ms). 실제 STT 지연이 아니라 화면 전환 타이머다.
 * (농인 트랙의 수어 인식은 실제 /recognize 호출로 대체되어 이 값을 더 쓰지 않는다.)
 */
export const MOCK_RECOGNITION_DELAY_MS = 2500;

/**
 * 청인 트랙 "수어로 보기" 화면의 인식 문장 목업 (피그마 시안 예시 문장 그대로).
 * STT 미구현이라 음성/키보드 입력과 무관하게 이 문장을 표시한다.
 */
export const MOCK_RECOGNIZED_SPEECH = '안녕하세요, 반갑습니다.';

/**
 * 검출 안내 문구("어깨/손이 화면에 들어오게 해주세요") 전환 지연(ms).
 * 검증되지 않은 프로토타입 임시값 — 한두 프레임 검출 누락에 안내가 깜빡이는 것만 막는 용도다.
 */
export const DETECTION_GUIDE_DELAY_MS = 1200;

/**
 * rejected/low_quality 인라인 배너의 자동 소멸 시간(ms).
 * 검증되지 않은 프로토타입 임시값 — 읽을 시간은 주되 화면에 눌러앉지 않게 하는 용도다.
 * 다음 캡처를 시작하면 시간과 무관하게 즉시 사라진다.
 */
export const RESULT_NOTICE_AUTO_DISMISS_MS = 6000;

/**
 * 음성 입력 "듣는 중" 상태의 타임아웃(ms). 시안 주석 "음성 인식 시간(10초 이내)로 인식하지
 * 못하면 다시 해달라는 알림창" 을 옮긴 프로토타입 임시값 — 10초가 확정값이라는 뜻이 아니다.
 */
export const VOICE_LISTEN_TIMEOUT_MS = 10_000;
