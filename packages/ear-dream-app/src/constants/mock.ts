/**
 * 와이어프레임 흐름 시뮬레이션용 임시값 · 목업 데이터.
 *
 * 여기 있는 값은 전부 미확정이다. 인식 파이프라인(T-08 이후)이 붙으면
 * 이 파일의 값은 그 값을 쓰는 코드와 함께 제거하거나 실제 값으로 대체한다.
 */

/**
 * "인식 중" 화면 체류 시간(ms). 실제 추론 지연이 아니라 화면 전환 타이머다.
 * 설계 문서의 허용 지연 목표치(미확정)와 아무 관련이 없다.
 */
export const MOCK_RECOGNITION_DELAY_MS = 1500;

/**
 * 후보 문장 목업 (피그마 시안의 예시 3개 그대로).
 * 후보 개수 N 은 사용자 검증 전까지 미확정이다 — 3이 확정값이라는 뜻이 아니다.
 */
export const MOCK_CANDIDATE_SENTENCES: readonly string[] = [
  '집에 가고 싶어요',
  '지하철 타려면 어디로 가요?',
  '역으로 어떻게 가요?',
];

/** 자유 발화 "다음 단어 추천" 목업 (피그마 시안 예시). 실제로는 서버 추천이 들어올 자리다. */
export const MOCK_NEXT_WORDS: readonly string[] = ['학교', '집', '회사'];

/**
 * 손이 계속 안 보일 때 예외 오버레이("손이 안 보여요")를 띄우기까지의 지연(ms).
 * 검증되지 않은 프로토타입 임시값 — 한두 프레임 검출 누락에 오버레이가 깜빡이는 것만 막는 용도다.
 */
export const HAND_LOST_OVERLAY_DELAY_MS = 1200;
