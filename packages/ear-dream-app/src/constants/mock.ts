/**
 * 흐름 시뮬레이션용 임시값 · 목업 데이터.
 *
 * 여기 있는 값은 전부 미확정이다. 인식 파이프라인(T-08 이후)이 붙으면
 * 이 파일의 값은 그 값을 쓰는 코드와 함께 제거하거나 실제 값으로 대체한다.
 *
 * 인식·문장 생성·음성 인식·수어 재생이 전부 실제 구현으로 대체되어 흐름 시뮬레이션
 * mock 은 남아 있지 않다. 남은 값들은 화면 연출용 지연 시간이다.
 */

/**
 * 검출 안내 문구("어깨/손이 화면에 들어오게 해주세요") 전환 지연(ms).
 * 검증되지 않은 프로토타입 임시값 — 한두 프레임 검출 누락에 안내가 깜빡이는 것만 막는 용도다.
 */
export const DETECTION_GUIDE_DELAY_MS = 1200;

/**
 * rejected/low_quality 인라인 배너의 자동 소멸 시간(ms).
 * 검증되지 않은 프로토타입 임시값 — 읽을 시간은 주되 화면에 눌러앉지 않게 하는 용도다.
 * 다음 캡처를 시작하면 시간과 무관하게 즉시 사라진다.
 */
export const RESULT_NOTICE_AUTO_DISMISS_MS = 6000;
