/**
 * 아바타의 색·치수 — **전부 눈으로 맞춘 프로토타입 임시값이다.**
 *
 * 조음 정확성 육안 검증에서 손대는 곳이 여기 하나가 되도록 모아 뒀다. 값을 왜 그렇게
 * 골랐는지는 각 상수 주석에 있고, 그 값을 쓰는 형상 계산은 `figure.ts` 에 있다.
 *
 * ⚠️ 실제 아바타 애셋이 정해지면 통째로 교체될 값이다. 확정값처럼 다루지 말 것.
 */

/**
 * 팔레트. 테마 토큰에는 피부색이 없다(UI 색이 아니라 그림 색이다).
 *
 * **살색은 시안 부품에서 뽑은 값이다.** 몸통·머리·팔이 그림으로 바뀐 뒤로 손만 벡터라,
 * 색이 어긋나면 손 하나만 다른 그림에서 온 것처럼 보인다. 시트가 함께 준 피부색
 * 견본(#F7DCC8 · #E9C3A7 · #D3A788)을 그대로 옮겼다 — 그림에서 뽑아 맞추지 않아도 된다.
 *
 * `shirt*` 와 `hair` 는 이제 **벡터 폴백에서만** 쓰인다 — 스프라이트를 못 받았을 때만
 * 보이는 색이라 시안과 맞출 이유가 없다.
 */
export const PAINT = {
  skinLight: '#f7dcc8',
  skinMid: '#e9c3a7',
  skinDark: '#d3a788',
  shadow: '#d59d7c',
  shirtLight: '#7b8ce0',
  shirtMid: '#4f46e5',
  shirtDark: '#3b32b4',
  hair: '#3a2418',
  /**
   * 손 윤곽선. 시안의 살색 그림자 톤이라 **잉크 선이 아니라 음영처럼** 읽힌다.
   *
   * 예전 값(#a4694c)은 벡터 몸 전체에 같은 굵기로 둘러 통일감을 만들던 선이었다.
   * 이제 선을 두르는 부위가 손뿐이라, 그때 색 그대로면 손만 만화가 된다.
   */
  outline: '#c99e8a',
  iris: '#4a3325',
  brow: '#4a3526',
  lip: '#d08a80',
  mouthInner: '#8a4c48',
} as const;

/** 어깨 너비 대비 굵기. 전부 프로토타입 임시값이고 눈으로 맞춘 것이다. */
export const LIMB = {
  torsoRound: 0.14,
  upperArmTop: 0.125,
  upperArmBottom: 0.095,
  forearmTop: 0.095,
  forearmBottom: 0.07,
  neck: 0.135,
} as const;

/**
 * 눈 바깥 꼬리 간격(eyeSpan) 대비 머리 크기.
 *
 * 데이터에 머리 윤곽이 없어 되짚어 그린다. 실측 인체 비례(머리 폭 ≈ 바깥 눈꼬리
 * 간격의 1.7배)로 맞춰 봤더니 **화면에서 얼굴이 작고 길어 인상이 굳어 보였다** —
 * 이목구비는 실측 위치라 얼굴만 커지면 상대적으로 오밀조밀해지기 때문이다.
 * 그래서 인체 비례보다 **머리를 크고 둥글게** 잡았다. 캐리커처가 사람보다 사람처럼
 * 읽히는 것과 같은 이유다.
 *
 * ⚠️ 시퀀스의 눈·입 위치는 실측이고 **머리만 추정**이라, 이 값이 어긋나면 그 차이가
 * 그대로 드러난다.
 */
export const HEAD = {
  halfWidth: 1.15,
  heightRatio: 1.3,
  centerDrop: 0.12,
  /**
   * 머리 기울기에서 무시할 폭(도)과 그 밖의 반영 비율.
   *
   * 눈 축 각도를 매 프레임 다시 재면 머리가 떤다 — 실측 방향 전환율이 37% 로
   * 대부분 측정 잡음이다(50% 가 백색 잡음의 지문). 문장 전체의 중앙값을 기준으로
   * 잡고 이 폭 안쪽은 눌러, **진짜 고개 기울임만** 남긴다.
   */
  angleDeadbandDeg: 3,
  angleGain: 0.6,
} as const;

/**
 * 이목구비를 그리는 방식 — **단순한 도형으로 그리고 데이터는 그 도형의 크기·각도만
 * 정한다.**
 *
 * 처음에는 실측 랜드마크를 그대로 이어 눈·입 윤곽을 그렸는데, 화면에서 눈이 몇 픽셀
 * 짜리라 어떤 배율로 키워도 "반쯤 감고 찌푸린 얼굴" 이 나왔다. 랜드마크의 미세한
 * 비대칭이 작은 얼굴에서는 전부 인상으로 읽히기 때문이다.
 *
 * 그래서 **표현은 도형이, 값은 데이터가** 맡는다 — 눈은 타원이고 그 높이가 개폐를,
 * 눈썹은 호이고 그 높이·기울기가 표정을, 입은 곡선/타원이고 그 벌어짐이 입모양을
 * 나른다. 깜빡임·눈썹 올림·입 벌림은 그대로 살아 있고 인상만 정돈된다.
 */
export const EYE = {
  /** 자리 — 머리 반폭/반높이 대비. */
  offsetX: 0.38,
  offsetY: 0.08,
  /** 뜬 눈은 **점** 하나. 크기는 고정이라 잡음이 낄 자리가 없다. */
  dotR: 0.055,
  /** 감은 눈은 **일자**. 점보다 조금 넓고 얇다. */
  closedHalf: 0.085,
  closedStroke: 0.045,
  /**
   * 이 아래로 내려가면 감은 것으로 본다 (눈꺼풀 간격 / 눈 가로폭).
   *
   * 실측 평상시 값이 0.25~0.37 이고 프레임간 잡음이 ±0.007 이라, 0.16 은 그 띠에서
   * 한참 아래다 — **평상시에는 절대 발동하지 않고** 진짜로 감을 때만 걸린다.
   * 덕분에 시간 평활 없이도 깜빡임이 떨리지 않는다.
   */
  closedBelow: 0.16,
  /** 눈썹 — 얇은 호. */
  browLift: 0.144,
  browWidth: 0.23,
  browArc: 0.075,
  browStroke: 0.052,
  /**
   * 눈썹 높이의 기준값과 무시할 폭 (눈썹–눈 간격 / 눈 가로폭).
   *
   * 실측 평상시 0.81~0.97 이 **거의 전부 측정 잡음**이다(프레임간 방향 전환율 52%,
   * 백색 잡음의 지문이 50%). 그 폭을 통째로 무시해야 눈썹이 꿈틀대지 않는다.
   * 진짜 눈썹 올림은 이 띠를 벗어나므로 그때만 움직인다.
   */
  browRest: 0.89,
  browDeadband: 0.1,
  browGain: 0.25,
  browMaxShift: 0.12,
  /**
   * 눈 조각 다섯 단(`wide`/`open`/`normal`/`half`/`closed`)을 고르는 값.
   *
   * `closedBelow` 는 절대값이지만(깜빡임은 평상시와 확연히 다르다), 나머지는 **문장
   * 전체의 중앙값에서 얼마나 벗어났는지**로 고른다. 실측 분포가 5~95백분위 0.229~0.313
   * 으로 매우 좁아서 절대 문턱을 박으면 눈이 매 프레임 커졌다 작아진다 — 그 폭의
   * 대부분이 측정 잡음이기 때문이다.
   *
   * `openDeadband` 안쪽은 전부 `normal` 이다. 그 밖으로 벗어날 때만 한 단씩 움직인다.
   * **전부 눈으로 맞춘 임시값이다** — 눈 열림이 잡음 위로 얼마나 의미를 나르는지
   * 이 레포에 실측이 없다.
   */
  openRest: 0.278,
  openDeadband: 0.035,
  openStep: 0.03,
} as const;

/**
 * 눈·눈썹·입 조각이 머리 그림 위 어디에 앉는지 — **머리 스프라이트 픽셀 좌표**다.
 *
 * 화면 좌표가 아니라 그림 좌표라서, 머리가 움직이든 기울든 다시 계산할 필요가 없다.
 * 조각을 머리와 같은 그룹 안에 그리면 머리의 변환이 그대로 따라온다.
 *
 * ## 자리는 시안이 정하고, 좌표는 표정만 바꾼다
 *
 * 값은 **1차 시트(`image.psd`)의 이목구비가 그려진 머리**에서 잰 것이다. 지금 쓰는
 * 머리 그림은 얼굴이 비어 있어 잴 데가 없으므로, 두 머리를 **귀**로 정합해 옮겼다
 * (가로 1.848 · 세로 1.676 — 등방이 아니라서 축마다 따로 쓴다). 참고 얼굴의 턱이
 * 옮긴 자리와 2% 안에서 맞아 정합이 맞다는 것을 확인했다.
 *
 * ⚠️ **인체 비례로 추정하지 말 것.** 한 번 그렇게 잡았다가 눈이 좁고 눈썹이 짧은,
 * 사람 같지 않은 얼굴이 나왔다 — 시안 인물은 실제 사람 비례가 아니고, 이목구비 사이의
 * 관계가 곧 그 인물의 인상이다. MediaPipe 좌표가 정하는 것은 **자리가 아니라 상태**다
 * (눈 뜬 정도 · 눈썹 높이 · 입 벌어짐). 자리를 좌표에 맞추면 얼굴이 매 프레임 다른
 * 사람이 된다.
 */
export const FACE_PART = {
  /** 눈 중심 두 점 (인물 기준 좌우). 머리 스프라이트를 꽂는 뼈이기도 하다. */
  eyeRight: [95.8, 211.1],
  eyeLeft: [199.2, 211.1],
  /** 눈 가로폭 — 조각의 눈초리 두 점이 이만큼 벌어지게 놓는다. */
  eyeWidth: 44.4,
  /** 눈썹 — 평상시 자리와 가로폭. */
  browRight: [94.9, 184],
  browLeft: [200.1, 184],
  browWidth: 72.1,
  /**
   * 눈썹이 평상시 자리에서 오르내릴 수 있는 폭(px, 위가 +).
   *
   * 시안의 눈썹–눈 간격이 28.5px 이라 그 3분의 1 남짓으로 잡았다. 눈썹 조각이 눈과
   * 분리되어 있어 단계로 갈아끼우지 않고 높이를 연속으로 옮긴다.
   */
  browTravel: 10,
  /**
   * 목 기둥의 중심 — 머리를 옷깃 구멍에 좌우로 맞추는 기준점이다.
   *
   * 머리는 **눈**에서, 몸통은 **어깨**에서 각자 자리를 잡기 때문에 인물이 고개를 살짝
   * 옆으로 두면 그 차이가 목–옷깃 이음매에 그대로 나온다(실측 3.3px / 목 폭 50px).
   * 이 점의 화면 x 를 몸통의 목 지점에 맞춰 머리를 좌우로만 밀어 준다.
   *
   * ⚠️ **위아래는 맞추지 않는다.** 시안의 목이 촬영 데이터보다 짧아서(눈–어깨 거리가
   * 눈 간격의 2.19배 vs 데이터 3.27배) 세로까지 목 지점에 박으면 머리가 어깨에
   * 파묻힌다. 세로는 눈이 정하고, 대신 목 기둥을 아래로 늘려 옷깃 안에 묻는다.
   */
  neckCenter: [148.5, 415],
  /** 코 — 콧구멍 바깥 끝 두 점. 머리 그림에 코가 없어서 조각으로 얹는다. */
  nose: { from: [132, 274], to: [163, 274] },
  /** 입꼬리 두 점. */
  mouth: { from: [114.2, 293.2], to: [180.8, 293.2] },
} as const;

/**
 * 얼굴을 좌표로 움직일지 — **기본은 끔(고정 표정)이다.**
 *
 * ## 왜 껐나
 *
 * 시퀀스의 얼굴 신호가 **측정 잡음과 구분되지 않는다.** 프레임간 방향 전환율을 재면
 * (300단어 중 120개 표본):
 *
 *   눈썹–눈 간격  0.626      눈 열림  0.631      입 벌어짐  0.621
 *   손목 y(대조군) 0.206
 *
 * 0.5 가 백색 잡음의 지문이고, 실제로 움직이는 손목은 0.21 이다. 얼굴 셋은 전부 0.5
 * 위쪽이라 **방향이 매 프레임 뒤집힌다** — 사람이 그렇게 움직일 수 없다.
 *
 * 이 값을 그대로 표정에 태우면 눈과 입이 초당 수십 번 바뀌어 얼굴이 떨린다. 없는
 * 정보를 지어내는 쪽이 아니라 **고정 표정**이 사실에 가깝다.
 *
 * ## 켜려면
 *
 * 얼굴 추적을 다시 뽑아 위 전환율이 손목 수준(0.2~0.3)으로 내려오면 켠다. 그때는
 * `EYE.openDeadband` · `browTravel` · `MOUTH.partedAbove` 를 그 데이터로 다시 잡아야 한다.
 * 조각과 상태 판정은 이미 다 있으므로 이 스위치만 켜면 된다.
 *
 * ⚠️ 비수지신호(눈썹·눈·입모양)는 수어의 문법 요소다. 지금 고정된 것은 **데이터가
 * 없어서**이지 필요 없어서가 아니다.
 */
export const FACE_ANIMATED = false;

export const MOUTH = {
  /** 입 높이는 머리 반높이 대비, 나머지는 머리 반폭 대비. */
  offsetY: 0.52,
  width: 0.287,
  restCurve: 0.098,
  stroke: 0.057,
  /** 이 이상 벌어지면 채운 입으로 그린다 (입꼬리 간격 대비). */
  openMin: 0.2,
  openFull: 0.55,
  openScale: 0.16,
  /**
   * 입 조각을 고르는 문턱 — 전부 `openMin` 과 같은 단위(입 벌어짐 / 입꼬리 간격)다.
   *
   * `openMin` 이 「벌어진 입」의 경계를 이미 잡고 있어서 그 값을 그대로 쓰고, 그보다
   * 살짝 벌어진 상태와 크게 벌어진 상태만 문턱을 더 뒀다. **눈으로 맞춘 임시값이다** —
   * 좌표에서 입모양을 몇 단계로 나누는 게 맞는지는 이 레포에 실측이 없다.
   */
  partedAbove: 0.1,
  wideAbove: 0.45,
} as const;

/**
 * 손 치수 — 손목~중지 MCP 거리(`unit`) 대비.
 *
 * 손이 얼굴 위로 올라오는 동작이 흔한데 둘 다 살색이라 **경계가 사라진다.** 그래서
 * 윤곽선을 두르고, 선이 생기는 만큼 실루엣도 손처럼 다듬는다 — 굵기가 일정한 막대
 * 다섯 개에 선만 두르면 장갑처럼 보인다.
 */
export const HAND = {
  /** 손바닥 — MCP 를 이은 다각형은 손금 선에 가까워서 살을 붙여야 손바닥이 된다. */
  palmWidth: 0.52,
  /** 손가락은 뿌리에서 끝으로 가늘어진다. 엄지는 굵고 짧다. */
  fingerBase: 0.17,
  fingerTip: 0.115,
  thumbBase: 0.2,
  thumbTip: 0.13,
} as const;

/**
 * 외곽선 두께 — 어깨 너비 대비.
 *
 * 손이 얼굴 위로 올라오면 둘 다 살색이라 경계가 사라진다. 그래서 손에는 선을 두른다.
 *
 * 그리는 법: 부위마다 같은 도형을 굵게 한 번 깔고 그 위에 제 색을 얹는다. 겹친
 * 도형들의 바깥선만 남고 내부 경계는 덮이므로 도형 합집합을 구할 필요가 없다.
 *
 * ⚠️ 예전에는 **모든 부위**에 같은 굵기로 둘렀다 — 손에만 두르면 손이 겉돌기 때문이다.
 * 몸이 스프라이트로 바뀌면서 선을 두를 부위가 손만 남았으니, 대신 `PAINT.outline` 을
 * 음영 톤으로 낮추고 굵기도 얇게 잡았다(0.016 → 0.011).
 */
export const OUTLINE_WIDTH = 0.011;

/**
 * 인물 둘레에 남길 여백 (잘라낸 범위 대비 비율).
 *
 * 0 이면 손끝·정수리가 화면 가장자리에 딱 붙어 잘린 것처럼 보인다. 위쪽을 더 주는 것은
 * 머리 윤곽·머리카락이 **데이터에 없는 추정 형상**이라 좌표 bbox 보다 위로 튀어나오기
 * 때문이다 — 여기를 줄이면 정수리가 잘린다.
 */
export const CROP_MARGIN = { x: 0.14, top: 0.16, bottom: 0.05 } as const;

/**
 * 팔 스프라이트 굵기 — 어깨 너비 대비.
 *
 * 그림의 원래 가로폭을 쓰지 않고 어깨 너비에 묶는다. 관절 간격에 묶으면 팔이 카메라를
 * 향할 때(원근 단축) 그림이 통째로 줄어 팔이 가늘어진다.
 *
 * ⚠️ 그래서 **그림 원래 비례보다 가늘다.** 시안의 위팔은 뼈 길이 대비 0.66 폭인데
 * 여기 값은 그 60% 남짓이다. 굵기를 관절에서 떼어 놓은 대가이고, 눈으로 맞춘 임시값이다.
 */
export const ARM_SPRITE = {
  /**
   * ⚠️ 몸통 배율(`BODY_SPRITE.torsoScale`)과 함께 봐야 한다. 몸통을 키우면 진동(팔 구멍)이
   * 어깨 관절보다 바깥으로 나가서, 소매 캡이 좁으면 그 구멍의 어두운 안쪽 면이 어깨
   * 위로 초승달처럼 비친다. 둘 중 하나만 건드리지 말 것.
   */
  upperWidth: 0.4,
  foreWidth: 0.3,
} as const;

/**
 * 몸통·머리 스프라이트 배율 — **가로 뼈(어깨선·눈초리선) 위에 등방으로 얹는다.**
 *
 * 팔과 다르게 길이·굵기를 따로 두지 않는다. 팔은 카메라를 향할 때 관절 간격이 줄어드는
 * 원근 단축을 굵기와 분리해야 했지만, 몸통과 머리는 뼈가 화면과 거의 나란해서 그 문제가
 * 없다. 등방이라 그림이 눌리거나 늘어나지 않는다.
 *
 * 대신 **뻣뻣해진다** — 벡터 몸통은 어깨·엉덩이 네 점이 데이터대로 변형됐지만
 * 스프라이트는 회전·확대만 된다. 2D 좌표로는 몸통 비틀림을 복원할 수 없으니
 * (손바닥 방향을 표현할 수 없는 것과 같은 한계) 지금은 이 교환이 맞다.
 *
 * 값은 **뼈의 중점을 축으로** 그림 전체를 키우는 배수다. 1 이면 그림의 두 앵커가
 * 데이터의 두 점에 정확히 얹힌다. **전부 눈으로 맞춘 임시값이다.**
 */
export const BODY_SPRITE = {
  /** 몸통 — 그림의 진동(어깨 이음선) 간격을 데이터 어깨 너비에 그대로 맞춘다. */
  torsoScale: 1.1,
  /**
   * 머리 — 눈초리 간격 기준. 1 이면 그림의 눈이 데이터의 눈 자리에 정확히 온다.
   *
   * ⚠️ **1 이 곧 자연스러운 값은 아니다.** 시안 인물은 어깨 너비 대비 눈 간격이
   * 실제 사람보다 넓게(머리가 크게) 그려져 있다 — 시안에서 재면 어깨/눈 = 1.9 인데
   * 촬영 데이터에서는 3.7 이다(사람 실측도 4 언저리라 데이터 쪽이 맞다).
   * 그래서 몸통을 데이터에 맞추면 시안 머리가 상대적으로 작아 보인다. 이 값은 그
   * 간극을 눈으로 메우는 손잡이다.
   */
  headScale: 1.15,
  /**
   * 머리를 아래로 내리는 거리 — 눈 간격 대비. **보이는 목 길이를 줄이는 손잡이다.**
   *
   * 목 길이는 데이터가 정한다(눈 높이는 눈에서, 옷깃은 어깨에서). 그 간격이 시안
   * 인물보다 길어서 목이 길어 보이는데, 머리를 키워 턱을 내리면 머리까지 커진다.
   * 대신 머리만 눈 축을 따라 조금 내려 **크기는 그대로 두고 목만 줄인다.**
   * 눈이 데이터에서 그만큼 벗어나지만, 눈 랜드마크는 잡음이라(전환율 0.63) 잃는 게 없다.
   */
  headDrop: 0.16,
} as const;
