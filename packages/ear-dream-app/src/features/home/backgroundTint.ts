/**
 * 첫 화면 배경의 색 계약 — 배경 영상 위에 얹히는 보라 스크림과, 영상이 없을 때의 폴백색.
 *
 * 확정 디자인의 첫 화면은 영상만 깔린 게 아니다. 영상 위에 프레임 전체를 덮는
 * `brand/primary 60%` 레이어(피그마 Group 55)가 한 장 더 있고, 시안의 보라 톤은 거기서
 * 나온다. **이 스크림이 흰 워드마크의 대비를 만든다** — 실측상 영상 자체의 평균색은
 * 밝은 웜그레이(#b49a88)라 스크림 없이는 흰 글자가 묻힌다. 장식이 아니라 가독성 장치이니
 * 지우지 말 것.
 */

/** 배경 영상 위 스크림 — 피그마 Group 55 (brand/primary 60%). */
export const HOME_SCRIM_COLOR = 'rgba(79, 70, 229, 0.6)';

/**
 * 영상이 없거나(네이티브) 재생이 막혔을 때의 단색 폴백.
 * 영상 평균색(#b49a88)에 위 스크림을 미리 합성한 값이라 톤이 이어진다.
 */
export const HOME_FALLBACK_COLOR = '#7768c0';
