/**
 * 인식·문장 변환 요청의 응답 대기 상한.
 *
 * 이 시간 안에 응답이 없으면 요청을 abort 하고 failed 로 전이한다 — 서버 재시작 창 등에서
 * 응답이 영영 오지 않으면 pending("단어를 읽고 있어요")에 무한히 갇히는 것을 막는다.
 * 세그먼트/단어 열은 보존되므로 사용자는 재전송할 수 있다.
 *
 * 프로토타입 임시값 — 확정 아님. 서버 처리 자체는 ~100ms 수준이지만, /recognize 페이로드가
 * 얼굴 원본 메쉬 포함 수백 KB라 저속 네트워크 업로드와 저사양 환경을 감안해 넉넉히 잡았다.
 * NFR-01(허용 지연) 실측 후 조정한다.
 */
export const RECOGNIZE_TIMEOUT_MS = 15000;
