# 실제 휴대폰(Expo Go)에서 테스트할 때는 localhost 가 휴대폰 자신을 가리키므로
# 개발 PC 의 LAN IP 를 지정해야 한다. (macOS: ipconfig getifaddr en0)
# iOS 시뮬레이터만 쓴다면 이 파일 없이 기본값(http://localhost:8000)으로 동작한다.
EXPO_PUBLIC_API_URL=http://192.168.0.10:8000
