# Node / JS
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*
pnpm-debug.log*
*.log

# Expo / React Native
.expo/
.expo-shared/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
packages/ear-dream-app/ios/
packages/ear-dream-app/android/

# Python
.venv/
packages/ear-dream-api/var/
**/__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# 생성물 — pnpm generate:api-types 로 재생성한다
packages/core/openapi.json
packages/core/src/generated/

# 생성물 — pnpm setup:mediapipe 로 재생성한다
# WASM 런타임(약 44MB)과 손 랜드마크 모델(약 7.5MB). 재다운로드가 가능하고 라이선스가
# 별도인 바이너리라 public 레포에 커밋하지 않는다.
packages/ear-dream-app/public/mediapipe/

# 팀 내부용 메모 (public 레포이므로 커밋하지 않는다)
CLAUDE.local.md
packages/ear-dream-api/app/examples/*.json

# 실기기 모바일 웹 테스트용 로컬 https 인증서 (scripts/setup-https-cert.mjs)
# 개인 키이고 기계마다 LAN IP 가 달라 공유 대상이 아니다.
var/certs/

# 세션 인계·작업 메모 (public 레포이므로 커밋하지 않는다 — CLAUDE.local.md 와 같은 취지)
# 새 세션은 로컬에서 그대로 읽을 수 있다.
_workspace/

# 릴리스 업로드용 모델 번들 아카이브 (README 「모델 번들」의 tar 명령 산출물)
spoter300-pilot.tar.gz
